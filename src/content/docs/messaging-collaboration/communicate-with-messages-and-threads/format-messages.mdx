---
title: "تنسيق الرسائل "
description: "تعرف على كيفية استخدام شريط أدوات التنسيق وMarkdown لتنسيق رسائلك في منصة تعاون بالتفصيل الكامل."
---

import { Tabs, TabItem, Steps, Aside, Badge } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';

{/* استيراد كافة الصور المطلوبة للملف */}
import formattingToolbarGif from '../../../../images/message-formatting-toolbar.gif';
import messagesTable1 from '../../../../images/messagesTable1.png';
import Headings1 from '../../../../images/Headings1.png';
import Headings2 from '../../../../images/Headings2.png';
import syntaxGithub from '../../../../images/syntax-highlighting-github.png';
import syntaxSolDark from '../../../../images/syntax-highlighting-sol-dark.png';
import syntaxSolLight from '../../../../images/syntax-highlighting-sol-light.png';
import syntaxMonokai from '../../../../images/syntax-highlighting-monokai.png';
import icon76 from '../../../../images/icon-76x76.png';
import icon50x76 from '../../../../images/icon-50x76.png';
import blockQuotesImg from '../../../../images/blockQuotes.png';
import markdownTable1 from '../../../../images/markdownTable1.png';
import latexInline from '../../../../images/latex-inline.png';
import latexCodeblock from '../../../../images/latex-codeblock.png';
import FAIcon from "../../../../components/FAIcon.astro";

## <FAIcon name="pen-ruler"/>استخدم شريط أدوات تنسيق الرسائل 
بدءًا من الإصدار **v7.0** من منصة تعاون، يمكنك تنسيق رسائلك باستخدام شريط أدوات تنسيق الرسائل دون الحاجة لكتابة ترميز Markdown يدويًا.

<Image 
  src={formattingToolbarGif} 
  alt="شريط أدوات تنسيق الرسائل، المتاح بدءًا من منصة تعاون v7.0، لا يتطلب بنية Markdown، ويجعل تنسيق نص الرسالة سريعًا وسهلاً." 
  style={{ width: '100%', maxWidth: '600px', borderRadius: '8px', display: 'block', margin: '20px auto' }} 
/>

| خيارات التنسيق | الأيقونات |
| :--- | :--- |
| **تنسيق النص (عريض، مائل، أو يتوسطه خط)** | <FAIcon name="bold"/> <FAIcon name="italic"/> <FAIcon name="strikethrough"/> |
| **إضافة العناوين، الروابط، أو المرفقات** | <FAIcon name="heading"/> <FAIcon name="link"/> <FAIcon name="paperclip"/> |
| **قائمة مرقمة، قائمة منقطة، اقتباس، أو نص برمجيد** | <FAIcon name="list-ol"/> <FAIcon name="list"/> <FAIcon name="quote-left"/> <FAIcon name="code"/> |
| **إضافة الرموز التعبيرية أو الصور المتحركة** | <FAIcon name="smile"/> <FAIcon name="icons"/> |
| **تحديد أولوية الرسالة** | <FAIcon name="exclamation"/> |

راجع معاينة لكيفية ظهور تنسيق الرسالة بعد إرسالها عن طريق اختيار أيقونة **إظهار/إخفاء المعاينة** <FAIcon name="eye"/>. اختر الأيقونة مرة أخرى للعودة إلى المسودة.
<Aside type="note">
- قم بإخفاء خيارات التنسيق عن طريق اختيار أيقونة **إظهار/إخفاء التنسيق (Show/Hide Formatting)** <FAIcon name="font"/>، واخترها مرة أخرى لإظهار الخيارات.
- يمكنك التحكم في ما إذا كان سيتم عرض تنسيق المنشور داخل محرر تنسيق الرسائل؛ عند تعطيله، سيظهر النص الخام. راجع [تخصيص القنوات](/end-user-guide/preferences/manage-advanced-options) لمزيد من التفاصيل.
</Aside>
----
##  استخدام Markdown 

يمكنك أيضًا تنسيق الرسائل باستخدام Markdown للتحكم في [نمط النص](#نمط-النص)، و [الروابط](#الروابط)، و [العناوين](#العناوين)، و [القوائم](#القوائم)، و [كتل الكود](#كتل-الكود)، و [الكود المضمن](#الكود-المضمن-in-line-code)، و [الصور المضمنة](#الصور-المضمنة-in-line-images)، و [الخطوط الأفقية](#الخطوط-الأفقية)، و [اقتباسات الكتل](#اقتباسات-الكتل)، و [الجداول](#الجداول)، و [الصيغ الرياضية](#الصيغ-الرياضية). استخدام Markdown سهل: اكتب رسالتك ثم استخدم قواعد التنسيق.

<Image 
  src={messagesTable1} 
  alt="يتحكم تنسيق Markdown في مظهر وملمس الرسائل النصية." 
  style={{ width: '100%', maxWidth: '700px', borderRadius: '8px', display: 'block', margin: '20px auto' }} 
/>

### نمط النص 

يمكنك استخدام `_` أو `*` حول كلمة أو عبارة لجعلها مائلة، أو `__` أو `**` لجعلها غامقة.

<Aside type="note">
يتم دعم اختصارات لوحة المفاتيح الشائعة: لجعل النص غامقًا، اضغط على `Ctrl`+`B` على Windows و Linux، أو `⌘`+`B` على Mac. لجعل النص مائلاً، اضغط على `Ctrl`+`I` على Windows و Linux، أو `⌘`+`I` على Mac.
</Aside>

- `*italics*` (أو `_italics_`) يظهر كنص مائل
- `**bold**` يظهر كنص غامق
- `***bold-italic***` يظهر كنص غامق ومائل
- `~~strikethrough~~` يظهر كنص مشطوب

### الروابط (Links)

<Aside type="note">
قم بتنسيق النص المحدد كرابط بالضغط على `Ctrl`+`K` على Windows و Linux، أو `⌘`+`K` على Mac.
</Aside>

#### روابط القنوات 

أنشئ رابطًا إلى قناة عامة بكتابة `~` متبوعة باسم القناة (على سبيل المثال، `~roadmap`). يتم عرض أسماء القنوات الخاصة للأعضاء المصرح لهم فقط.





#### الروابط المعنونة 
أنشئ رابطًا معنونًا عن طريق وضع النص المراد في أقواس مربعة `[]` متبوعًا بالرابط في أقواس دائرية `()`.

`[تحقق من منصة تعاون!](https://mattermost.com/)`

تظهر كـ: [تحقق من منصة تعاون!](https://mattermost.com/)

### العناوين 

أنشئ عنوانًا بكتابة `#` ومسافة قبل النص. للعناوين الأصغر، استخدم المزيد من رموز `#`.

```text
## Large Heading
### Smaller Heading
#### Even Smaller Heading
```

تظهر كـ:

![Large Heading](../../../../images/Headings1.png)

بدلاً من ذلك، يمكنك استخدام أسطر تحتية من `===` أو `---` لإنشاء العناوين.

```text
Large Heading
-------------
```

تظهر كـ:

![Smaller Heading](../../../../images/Headings2.png)

### القوائم (Lists)

أنشئ قائمة باستخدام `*` أو `-` أو `+` كعلامات نقطية. استخدم مسافتين لإنشاء مسافة بادئة للعناوين الفرعية.

```text
* item one
- item two
  + item two sub-point
```

تظهر كـ:

- item one
- item two
  - item two sub-point

استخدم الأرقام لإنشاء قائمة مرتبة:

```text
1. Item one
1. Item two
1. item three
```

تظهر كـ:

1. Item one
2. Item two
3. Item three


استخدم الاقواس لإنشاء قائمة خيارت صح او خطا: 

```text
- [ ] Item one
- [ ] Item two
- [x] Completed item
```
تظهر كـ:
- [ ] Item one
- [ ] Item two
- [x] Completed item
### كتل الكود 

أنشئ كتل كود باستخدام ثلاث علامات اقتباس خلفية (backticks) ` ``` ` على سطر جديد قبل الكود وبعده. بدلاً من ذلك، يمكنك إنشاء كتل كود عن طريق إضافة أربع مسافات بادئة لكل سطر من الكود.

لتمكين تمييز بناء الجملة (syntax highlighting) للغات مختلفة، أدخل اسم اللغة المقابل بعد علامات الاقتباس الخلفية الثلاث الأولى.

يدعم منصة تعاون اللغات والأسماء المستعارة التالية:

| اللغة | الأسماء المستعارة |
| :--- | :--- |
| ActionScript | actionscript, as, as3 |
| AppleScript | applescript |
| Bash | bash, sh |
| Clojure | clojure |
| CoffeeScript | coffescript, coffee, coffee-script |
| C/C++ | cpp, c++, c |
| C# | cs, c#, csharp |
| CSS | css |
| D | d, dlang |
| Dart | dart |
| Delphi | delphi |
| Diff | diff, patch, udiff |
| Django | django |
| Dockerfile | dockerfile, docker |
| Elixir | elixir, ex, exs |
| Erlang | erlang, erl |
| Fortran | fortran |
| F# | fsharp |
| G-Code | gcode |
| Go | go, golang |
| Groovy | groovy |
| Handlebars | handlebars, hbs, mustache |
| Haskell | haskell, hs |
| Haxe | haxe |
| Java | java |
| JavaScript | javascript, js |
| JSON | json |
| Julia | julia, jl |
| Kotlin | kotlin |
| LaTeX | latex, tex |
| Less | less |
| Lisp | lisp |
| Lua | lua |
| Makefile | makefile, make, mf, gnumake, bsdmake |
| Markdown | markdown, md, mkd |
| Matlab | matlab, m |
| Objective C | objectivec, objective_c, objc |
| OCaml | ocaml |
| Perl | perl, pl |
| Pascal | pascal, pas |
| PostgreSQL | pgsql, postgres, postgresql |
| PHP | php, php3, php4, php5 |
| PowerShell | powershell, posh |
| Puppet | puppet, pp |
| Python | python, py |
| R | r, s |
| Ruby | ruby, rb |
| Rust | rust, rs |
| Scala | scala |
| Scheme | scheme |
| SCSS | scss |
| Smalltalk | smalltalk, st, squeak |
| SQL | sql |
| Stylus | stylus, styl |
| Swift | swift |
| Text | text |
| TypeScript | typescript, ts, tsx |
| VB.Net | vbnet, vb, visualbasic |
| VBScript | vbscript |
| Verilog | verilog |
| VHDL | vhdl |
| HTML, XML | html, xml |
| YAML | yaml, yml |

مثال:

````text
``` go
package main
import "fmt"
func main() {
    fmt.Println("Hello, 世界")
}
```
````

تظهر كـ:

**سمة GitHub **

![تمييز بناء الجملة في GitHub](../../../../images/syntax-highlighting-github.png)

**سمة Solarized Dark (Solarized Dark Theme)**

![تمييز بناء الجملة الداكن](../../../../images/syntax-highlighting-sol-dark.png)

**سمة Solarized Light (Solarized Light Theme)**

![تمييز بناء الجملة الفاتح](../../../../images/syntax-highlighting-sol-light.png)

**سمة Monokai (Monokai Theme)**

![تمييز بناء الجملة Monokai](../../../../images/syntax-highlighting-monokai.png)

### الكود المضمن (In-line code)

أنشئ نص كود أحادي المسافة مضمنًا عن طريق إحاطته بعلامات اقتباس خلفية (backticks) `` ` ``. لا تستخدم علامات الاقتباس المفردة `'`.

```text
`monospace`
```

تظهر كـ: `monospace`.

### الصور المضمنة 

الصور المضمنة هي صور تُضاف داخل سطور النص. يمكنك التحكم في ما إذا كانت جميع الصور المضمنة التي يزيد ارتفاعها عن 100 بكسل سيتم طيها أو توسيعها تلقائيًا في الرسائل من خلال تعيين [تفضيل المستخدم](/end-user-guide/preferences/manage-your-display-options)، أو باستخدام أوامر الشرطة المائلة `/collapse` و `/expand`.

لإضافة صور مضمنة إلى النص، استخدم علامة تعجب `!` متبوعة بـ `نص بديل` (alt text) في أقواس مربعة `[]` ثم `رابط الصورة` في أقواس دائرية `()`. يمكنك إضافة نص عند التمرير بعد الرابط عن طريق وضع النص بين علامتي اقتباس `" "`.

مثال:

```text
![alt text](URL of image "Hover text")
```

إذا كان ارتفاع الصورة الأصلية أكثر من 500 بكسل، يقوم منصة تعاون بتعيين ارتفاع الصورة عند 500 بكسل ويضبط العرض للحفاظ على نسبة العرض إلى الارتفاع الأصلية.

يمكنك تعيين عرض وارتفاع الصورة المعروضة بعد عنوان URL للصورة باستخدام علامة التساوي `=` متبوعة بقيم لكل من العرض والارتفاع `##x##`. إذا قمت بتعيين العرض فقط، فسيقوم منصة تعاون بضبط الارتفاع للحفاظ على نسبة العرض إلى الارتفاع الأصلية.

:::caution
لا تدعم التطبيقات الأصلية (المنصات المحمولة) العرض والارتفاع الثابتين وستعرض الصورة بالحجم الكامل.
:::

أمثلة:

```text
![mattermost-icon-76x76](../../images/icon-76x76.png)
![mattermost-icon-50x76](../../images/icon-50x76.png)
```

#### صورة مضمنة مع نص عند التمرير 
```text
![منصة تعاون](../images/icon-76x76.png "منصة تعاون Icon")
```

تظهر كـ:

> ![منصة تعاون](../../../../images/icon-76x76.png)

#### صورة مضمنة مع رابط 

:::note
يلزم وجود مجموعة إضافية من الأقواس المربعة `[]` حول النص البديل، وأقواس دائرية `()` حول رابط الصورة.
:::

```text
[![منصة تعاون](../images/icon-76x76.png)](https://github.com/mattermost/mattermost)
```

تظهر كـ:

> [![image](../../../../images/icon-76x76.png)](https://github.com/mattermost/mattermost)

#### صورة مضمنة مع عرض وارتفاع ثابتين 

مثال: صورة مضمنة بعرض 50 بكسل وارتفاع 76 بكسل.

```text
![منصة تعاون](../images/icon-76x76.png =50x76 "منصة تعاون Icon")
```

تظهر كـ:
> [![image](../../../../images/icon-50x76.png)](https://github.com/mattermost/mattermost)


#### صورة مضمنة مع عرض ثابت 

مثال: صورة مضمنة بعرض 50 بكسل حيث يقوم النظام بضبط الارتفاع للحفاظ على نسبة العرض إلى الارتفاع الأصلية.

```text
![منصة تعاون](../images/icon-76x76.png =50 "منصة تعاون Icon")
```

تظهر كـ:

> [![image](../../../../images/icon-76x76.png)](https://github.com/mattermost/mattermost)

### الخطوط الأفقية 

أنشئ خطًا باستخدام ثلاث رموز من `*` أو `_` أو `-`.

`***`

تظهر كـ:

---

### اقتباسات الكتل 

أنشئ اقتباسات كتل باستخدام رمز `>`.

`> block quotes` تظهر كـ:

![image](../../../../images/blockQuotes.png)

### الجداول (Tables)

أنشئ جدولاً عن طريق وضع خط متقطع `---` أسفل سطر الترويسة، ثم فصل كل عمود باستخدام الرموز `|`. لا تحتاج الأعمدة إلى المحاذاة بدقة. اختر كيفية محاذاة أعمدة الجدول من خلال تضمين النقطتين الرأسيتين `:` داخل سطر الترويسة.

```text
| Left-Aligned  | Center Aligned  | Right Aligned |
| :------------ |:---------------:| -----:|
| Left column 1 | this text       |  $100 |
| Left column 2 | is              |   $10 |
| Left column 3 | centered        |    $1 |
```

تظهر كـ:

![عينة جدول Markdown](../../../../images/markdownTable1.png)

:::note
النص متعدد الأسطر في خلية الجدول غير مدعوم باستخدام علامات HTML مثل `<br>` أو `<br/>`.
:::

### الصيغ الرياضية 

<div name="tab">

استخدام LaTeX المضمن 
يمكنك إنشاء صيغ تظهر بشكل مضمن باستخدام LaTeX. استخدم رمز علامة الدولار ($) في بداية ونهاية كل صيغة.

:::note
هذه الميزة [معطلة افتراضيًا](/administration-guide/configure/site-configuration-settings#enable-inline-latex-rendering). اتصل بمسؤول النظام لتمكين هذا الإعداد في **وحدة تحكم النظام (System Console) > تكوين الموقع (Site Configuration) > المنشورات (Posts)** لاستخدام هذه الميزة.
:::

```text
$X_k = \sum_{n=0}^{2N-1} x_n \cos \left[\frac{\pi}{N} \left(n+\frac{1}{2}+\frac{N}{2}\right) \left(k+\frac{1}{2}\right) \right]$
```

تظهر كـ:

![عينة معادلة رياضية LaTeX مضمنة.](../../../../images/latex-inline.png)

</div>

<div name="tab">

استخدام LaTeX في كتل الكود 
أنشئ صيغًا ككتل كود باستخدام LaTeX في [كتل الكود](#كتل-الكود-code-blocks) من نوع `latex`.

:::note
هذه الميزة [معطلة افتراضيًا](/administration-guide/configure/site-configuration-settings#enable-latex-code-block-rendering). اتصل بمسؤول النظام لتمكين هذا الإعداد في **وحدة تحكم النظام (System Console) > تكوين الموقع (Site Configuration) > المنشورات (Posts)** لاستخدام هذه الميزة.
:::

````text
```latex
X_k = \sum_{n=0}^{2N-1} x_n \cos \left[\frac{\pi}{N} \left(n+\frac{1}{2}+\frac{N}{2}\right) \left(k+\frac{1}{2}\right) \right]
```
````

تظهر كـ:

![عينة معادلة رياضية في كتلة كود LaTeX.](../../../../images/latex-codeblock.png)

</div>

